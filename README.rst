reduc.tinydns
=============

reduc.tinydns is an utility for Dan Berstein' djbdns tinydns server. It generates a data zone file from static
files in the disk, plus files downloaded from other DNS servers. It processes these files according to a configuration
file before generating the resulting data file.